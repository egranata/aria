[workspace]
members = [
    "aria-bin",
    "compiler-lib",
    "native-libs/*",
    "opcodes-lib",
    "parser-lib",
    "test-bin",
    "vm-lib",
]
resolver = "2"

[profile.release]
codegen-units = 1
lto = "fat"
