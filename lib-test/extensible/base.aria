# SPDX-License-Identifier: Apache-2.0
struct Base {
    type func new(x,y) {
        return alloc(This){
            .x = x,
            .y = y,
        };
    }

    func swap() {
        return Base.new(this.y, this.x);
    }
}
