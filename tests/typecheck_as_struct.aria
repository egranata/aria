# SPDX-License-Identifier: Apache-2.0
struct Foo {
    type func new() {
        return alloc(This);
    }

    instance func truth() {
        return true;
    }
}

func main() {
    val f: Foo = Foo.new();
    assert(f.truth());
}
