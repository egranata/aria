# SPDX-License-Identifier: Apache-2.0
struct Foo {
    operator [](...) {
        return varargs.len();
    }
}

func main() {
    val f = alloc(Foo);

    assert f[1,2,3] == 3;
    assert f[3,2,1] == 3;

    assert f[0] == 1;
    assert f[32, 33] == 2;
}