# SPDX-License-Identifier: Apache-2.0
val x = 123;
val y = 100;

func main() {
    val x = 321;

    func foo(n) {
        return n + x + y; 
    }

    val bar = |n| => n + x + y;

    assert foo(3) == 424;
    assert bar(3) == 424;
}
